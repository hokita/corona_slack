FROM golang:1.14

ARG SLACK_WEBHOOK_URL

ENV GOBIN /go/bin
ENV GO111MODULE=on
ENV GOPATH=
ENV SLACK_WEBHOOK_URL=$SLACK_WEBHOOK_URL

WORKDIR /go
ADD . /go

RUN go build -o main main.go

CMD ["./main"]
